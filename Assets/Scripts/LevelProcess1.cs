using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelProcess1 : MonoBehaviour
{
    //LevelController levelController;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }

    //IEnumerator CookingProcess()
    //{
    //    if (!levelController.gameFail)
    //    {
    //        yield return StartCoroutine(levelController.DishMove("Right", 3));
    //    }

    //    if (!levelController.gameFail)
    //    {
    //        yield return StartCoroutine(levelController.CheckProcess(0));
    //    }

    //    if (!levelController.gameFail)
    //    {
    //        yield return StartCoroutine(levelController.DishMove("Right", 2));
    //    }

    //    if (!levelController.gameFail)
    //    {
    //        yield return StartCoroutine(levelController.CheckProcess(1));
    //    }

    //    if (!levelController.gameFail)
    //    {
    //        yield return StartCoroutine(levelController.DishMove("Right", 3));
    //    }

    //    if (!levelController.gameFail)
    //    {
    //        levelController.FinishCooking();
    //    }
    //}
}
